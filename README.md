# Safe-Booking
This repository implements a local booking system where users can visit, take up a slot, and reserve it, while the backend displays the bookings clearly. It is a simulated real world system similar to what companies use.
